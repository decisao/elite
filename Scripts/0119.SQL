/*
** CENTROS NOS MOVIMENTOS
*/

SET TERM !;

ALTER TABLE MOVIMENTOS ADD CODCENTRO INTEIRO_VALIDO!

UPDATE
  MOVIMENTOS
SET
  CODCENTRO = 1
WHERE
  ES = 2!

UPDATE
  MOVIMENTOS
SET
  CODCENTRO = 2
WHERE
  ES IN (0, 1)!  
  
ALTER TABLE MOVIMENTOS
ADD CONSTRAINT MOVIMENTOS_FK10
FOREIGN KEY (CODCENTRO)
REFERENCES CENTROS(CODIGO)
ON UPDATE CASCADE!
  
INSERT INTO SCRIPTVER(NUMERO, DATA) VALUES (119, CURRENT_TIMESTAMP)!