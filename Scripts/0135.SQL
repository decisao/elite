/*
** INICIALIZAÇÃO DO PLANO DE CONTAS
*/

SET TERM !;

INSERT INTO
  SEQUENCIA(
    CODIGO,
    TABELA,
    SEQUENCIA)
  VALUES(
    43,
    'PLANCONTAS',
    10)!
    
ALTER TABLE PAGAMENTOS ADD CODPLANO INTEIRO!

ALTER TABLE PAGAMENTOS
ADD CONSTRAINT PAGAMENTOS_FK06
FOREIGN KEY (CODPLANO)
REFERENCES PLANCONTAS(CODIGO)
ON UPDATE CASCADE!
    
INSERT INTO SCRIPTVER(NUMERO, DATA) VALUES (135, CURRENT_TIMESTAMP)!